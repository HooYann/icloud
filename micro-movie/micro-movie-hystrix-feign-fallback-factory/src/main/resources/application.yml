server:
  port: 18042

logging:
  root: DEBUG

spring:
  application:
    name: micro-movie-hystrix-feign-fallback-factory

eureka:
  client:
    service-url:
      defaultZone: http://localhost:18000/eureka/
  instance:
    prefer-ip-address: true

feign:
  hystrix:
    enabled: true