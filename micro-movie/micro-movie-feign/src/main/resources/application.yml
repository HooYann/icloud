server:
  port: 18027

logging:
  root: DEBUG

spring:
  application:
    name: micro-movie-feign

eureka:
  client:
    service-url:
      defaultZone: http://localhost:18000/eureka/
  instance:
    prefer-ip-address: true
