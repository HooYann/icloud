server:
  port: 18034

logging:
  root: DEBUG

spring:
  application:
    name: micro-movie-feign-upload

eureka:
  client:
    service-url:
      defaultZone: http://localhost:18000/eureka/
  instance:
    prefer-ip-address: true
