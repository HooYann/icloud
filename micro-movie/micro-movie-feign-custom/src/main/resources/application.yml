server:
  port: 18028

logging:
  root: DEBUG

spring:
  application:
    name: micro-movie-feign-custom

eureka:
  client:
    service-url:
      defaultZone: http://localhost:18000/eureka/
  instance:
    prefer-ip-address: true
