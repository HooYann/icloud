server:
  port: 18040

logging:
  root: DEBUG

spring:
  application:
    name: micro-movie-hystrix-ribbon

eureka:
  client:
    service-url:
      defaultZone: http://localhost:18000/eureka/
  instance:
    prefer-ip-address: true
