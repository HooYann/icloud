server:
  port: 18020

logging:
  root: DEBUG

spring:
  application:
    name: micro-movie-ribbon

eureka:
  client:
    service-url:
      defaultZone: http://localhost:18000/eureka/
  instance:
    prefer-ip-address: true
